{
  "name": "trashcore-hub",
  "description": "Baileys-powered WhatsApp pairing portal that generates pairing codes and DM's session IDs securely.",
  "repository": "https://github.com/yourusername/trashcore-hub",
  "keywords": ["node", "express", "whatsapp", "baileys", "heroku", "trashcore"],
  "env": {
    "SESSIONID": {
      "description": "Optional session ID if you want to pre-load credentials",
      "required": false
    }
  },
  "buildpacks": [
    {
      "url": "heroku/nodejs"
    }
  ],
  "formation": {
    "web": {
      "quantity": 1,
      "size": "eco"
    }
  },
  "scripts": {
    "postdeploy": "echo '🚀 Trashcore Hub deployed successfully!'"
  }
}
